server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://gateway:8000/api/;
    }

    location / {
        try_files $uri $uri/ $uri.html =404;
    }
}
